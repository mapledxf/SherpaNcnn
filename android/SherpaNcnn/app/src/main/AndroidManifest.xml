<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <permission
        android:name="com.k2fsa.sherpa.ncnn.BIND_RECOGNITION_SERVICE"
        android:label="Bind Recognition Service" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SherpaNcnn"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <service android:name=".AsrService"
            android:exported="true"
            android:permission="com.k2fsa.sherpa.ncnn.BIND_RECOGNITION_SERVICE">
            <intent-filter>
                <!-- 添加一个 action，客户端 APK 将使用它来绑定 Service -->
                <action android:name="com.k2fsa.sherpa.ncnn.RECOGNITION_SERVICE_ACTION" />
            </intent-filter>
        </service>
    </application>

</manifest>